import { StyleSheet } from "react-native";

export default StyleSheet.create({
  rightIcon: {
    right: -5,
  },

  leftIcon: {
    left: -5,
  },

  input: {
    padding: 20,
  },

  characterCounter: {
    fontSize: 11,
    right: 22,
    position: "absolute",
    bottom: 20,
    paddingRight: 2,
    paddingBottom: 2,
  },

  button: {
    padding: 15,
    margin: 10,
  },
});
